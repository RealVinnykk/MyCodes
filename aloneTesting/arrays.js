
function pokemon(nome, level) {
    nome,
    level,
    ATP = level * 1.2,
    HP = level * 1.3,
    console.log("Name: " + nome +  "\nLevel: "+ level +"\nHealth Points: "+ HP + "\nAttack Points: " + ATP)
}

let ippo = (pokemon("Ippo", 30))

console.log(ippo)